<div class="full">
  <div class="hero-content">
    <div class="hero-text">
      <h1>Adam Buschemia<br />Business Consultant</h1>
      <p>
        Get help from Alex Moore, a professional business coach with advanced
        experience on growth and business scaling.
      </p>
    </div>
    <div class="hero-img">
      <img src="../img/Photo.avif" alt="" />
    </div>
  </div>
</div>

<style>
  .full {
    grid-column: full;
    background-color: var(--color-primary-3);
    position: relative;
    z-index: 0;
  }

  .hero-content {
    padding-inline-start: max(1rem, 50% - var(--content) / 2);
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
  }

  .hero-text {
    max-width: 500px;
    color: var(--color-secondary-2);
  }

  .hero-text h1 {
    margin: 0 0 1rem;
    color: var(--color-secondary-2);
  }

  .hero-text p {
    margin: 0;
    color: var(--color-secondary-2);
  }

  .hero-img {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-img img {
    width: 100%;
    height: auto;
    object-fit: cover;
  }
</style>
